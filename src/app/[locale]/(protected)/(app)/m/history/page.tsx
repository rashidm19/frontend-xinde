import { MobileDashboardPage } from '@/app/[locale]/(protected)/(app)/dashboard/_components/MobileDashboardPage';

export default function HistoryPage() {
  return <MobileDashboardPage activeTab='history' />;
}
